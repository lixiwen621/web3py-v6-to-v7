schema_version: "1.0"

name: "web3py-v6-to-v7"
version: "0.1.0"
description: "Migrate web3.py v6 codebases to v7 — middleware renames, removals, API updates, and breaking changes"
author: "Coder"
license: "MIT"
workflow: "workflow.yaml"
category: "migration"

targets:
  languages: ["python"]

keywords: ["breaking-change", "upgrade", "v6-to-v7", "web3py", "web3.py"]

registry:
  access: "public"
  visibility: "public"
